function getListProduct(t, a, o) {
    $.ajax({ type: "GET", url: "http://localhost/missionpro/loadmore/getmoreproducthot?type=" + t + "&page=" + a, dataType: "html" }).done(function (e) {
        setDataProduct(t, e, a + 1 < o);
    });
}
function setDataProduct(t, a, o) {
    1 === t
        ? ($(".hot-flex").append(a),
          o || $("#hotLoadmore").css("display", "none"),
          $(".hot-product .fs-waiting").fadeOut(250, function () {
              centerPaging(), $(".hot-product .fs-waiting").removeClass("show-loading"), (isLoading = !0);
          }))
        : 2 === t &&
          ($(".suggest-flex").append(a),
          o || $("#suggestLoadmore").css("display", "none"),
          $(".suggest .fs-waiting").fadeOut(250, function () {
              onScroll(), centerPaging(), $(".suggest .fs-waiting").removeClass("show-loading"), (isLoading = !0);
          })),
        setClock();
}
function clickInBanner(t, a, o, e, n, i) {
    var s = "";
    s = 1 == i ? "Trái" : 2 == i ? "Phải" : "Giữa";
    var d = "";
    (d = 1 == n ? "Banner chính" : 2 == n ? "Banner quảng cáo trên" : "Banner quảng cáo dưới"), dataLayer.push({ event: "promotionClick", ecommerce: { promoClick: { promotions: [{ id: o, name: e, creative: d, position: s }] } } });
    var g = "";
    2 == a && (g = "_blank"), window.open(t, g);
}
$("#hotLoadmore").click(function () {
    isLoading &&
        ((isLoading = !1),
        $(".hot-product .fs-waiting").fadeIn(250, function () {
            $(".hot-product .fs-waiting").addClass("show-loading");
            let t = parseInt($("#hotLoadmore").attr("data-page-id"));
            getListProduct(1, t + 1, parseInt($("#hotLoadmore").attr("data-total-page"))), $("#hotLoadmore").attr("data-page-id", t + 1);
        }));
}),
    $("#suggestLoadmore").click(function () {
        isLoading &&
            ((isLoading = !1),
            $(".suggest .fs-waiting").fadeIn(250, function () {
                $(".suggest .fs-waiting").addClass("show-loading");
                let t = parseInt($("#suggestLoadmore").attr("data-page-id"));
                getListProduct(2, t + 1, parseInt($("#suggestLoadmore").attr("data-total-page"))), $("#suggestLoadmore").attr("data-page-id", t + 1);
            }));
    });
